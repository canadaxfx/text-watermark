# 开发日志 / Development Log

**项目**: 文本水印工具 (Text Watermark Tool)
**文件**: `watermark.html` — 单文件纯前端工具
**开发周期**: 2026年4月

---

## 版本历史

### v1.0 — 初始版本

**功能**:
- 基础括号标识水印：将名字/代号逐字插入文章
- 六种括号随机混搭
- 基础验证功能

---

### v1.1 — Unicode 组合标记（已废弃）

**尝试**: 在文字上叠加 Unicode 组合标记（U+0300–U+036F），将水印隐藏在字符上方/下方。

**结果**: 失败。微博发布后组合标记显示为方块 □，编辑器和已发布帖子均有此问题。

**决策**: 完全移除组合标记功能。

---

### v1.2 — 零宽字符（ZWC）隐藏水印层

**背景**: 需要一种不影响文章外观、且能在微博发布后保留的隐藏水印方案。

**技术方案**:
- 使用三种零宽字符编码二进制：
  - U+200B = bit 0
  - U+200C = bit 1  
  - U+200D = 分隔符/哨兵
- 编码格式：`8× U+200D（哨兵头）+ 8位长度 + 每字符16位`
- 将编码后的 ZWC 序列嵌入文章三处位置（头部、中部、尾部）

**验证**: 用户在微博发布含 ZWC 水印的文章，复制粘贴后用 `[...text].map(c => c.codePointAt(0).toString(16))` 检测，确认三种 ZWC 字符均完整保留。

**新增功能**:
- `zwcEncode(identity)` — 将标识编码为 ZWC 序列
- `zwcDecode(text)` — 从文本中提取并解码 ZWC 序列
- 验证标签页新增"零宽字符水印"检测区域

---

### v1.3 — 干扰项系统重设计

**原设计问题**: 干扰项插入随机假字符，但盗用者只需一条正则表达式删除所有括号内容即可清除全部标记，原文不受损。

**用户洞察**: "干扰项应该把原文中的词语括起来？"

**新设计**:
- 干扰项不再插入假字符，而是将原文中已有的字符用括号包裹
- 盗用者若删除所有括号内容 → 原文出现缺字，文意破损，暴露删改行为
- 若逐一判断哪些括号要保留 → 需手动重建全文，成本极高

**实现细节**:
- 干扰项回溯最近 1–2 个 `original` 类型的非标点字符，将其包裹
- 核心数据结构 `segments[]`，类型：`original / identity / decoy / custom`

---

### v1.4 — 干扰项密度改为"每 N 字一个"

**原设计**: 下拉菜单选择数量（1/2/3个）

**问题**: 固定数量对长文稀疏、对短文过密，用户希望按比例调节。

**新设计**: 数字输入框，含义为"每 N 字插入一个干扰项"
- 默认值：每 15 字一个
- 0 = 关闭干扰项
- 短文自动补偿：`Math.max(1, Math.round(textLen / decoyN))`

---

### v1.5 — 英文整词支持

**问题**: 英文名（如 Alice）被逐字拆分，生成 `【A】【l】【i】【c】【e】` 毫无意义。

**修复**: 新增 `tokenizeIdentity(identity)` 函数：
- 中文字符：逐字拆分（每字单独插入）
- 英文序列：整词保留（如 Alice → 【Alice】）
- 混合标识（如"小花Alice"）：中文逐字 + 英文整词

---

### v1.6 — 图片水印标签页

**背景**: 纯文字水印仍可被技术用户批量处理，图片形式从根本上阻止复制。

**功能**:
- Canvas 渲染文章为图片（750px 宽）
- 斜向水印文字铺满全图（45° 旋转，重复平铺）
- 可配置：水印文字、颜色（灰/红/蓝/黑）、深浅（浅/中/深）、正文字号

**实现**:
- `generateImage()` — 文字排版 + Canvas 渲染
- `wrapTextLines()` — 中文自动换行（按字符宽度计算）
- `downloadImage()` — 触发 PNG 下载

---

### v1.7 — 存证图片功能

**背景**: 用户需要保留原创证据，以便在版权争议时举证。

**功能**: 在"纯文字水印"标签页生成水印后，点击"生成存证图片"可下载一张：
- 括号标识位置用红色高亮
- 斜向水印叠加
- 底部标注生成时间戳
- 文件名含日期（如 `存证_肖潘达_20260420.png`）

**实现**: `saveEvidenceImage()` — 逐段渲染，identity 段用红色，decoy 段用橙色，其余黑色。

---

### v1.8 — UI / UX 优化

**改动**:
- **自动生成**: 输入停止 600ms 后自动更新结果（`scheduleGenerate()`），无需手动点击
- **Textarea 自适应高度**: `autoResize()` 监听 input 事件，自动撑高输入框
- **设置持久化**: 所有配置自动保存至 `localStorage`，下次打开自动恢复
- **高级设置折叠**: 干扰项密度、自定义警告语默认折叠，简化初始界面
- **标签页重命名**:
  - `图片水印` → `文字转水印图片`
  - `文字水印` → `纯文字水印`
- **标签页排序**: 图片标签页置前（更直观）
- **字段顺序调整**: 水印标识移至原始文本之后（更符合操作流程）

---

### v1.9 — 说明标签页 & 版权声明

**新增**:
- 第四个标签页"说明"：工具介绍、三层保护机制说明、存证使用指南、版权声明
- HTML `<head>` 版权注释
- README 完整重写（`watermark_README.md`）
- 许可证说明：个人非商业用途免费，须注明原作者，禁止商业使用

---

## 技术选型说明

| 决策 | 原因 |
|------|------|
| 单 HTML 文件，零依赖 | 用户无需安装，离线可用，易于分发 |
| Mulberry32 PRNG | 确定性随机：相同输入产生相同输出，便于重现和验证 |
| ZWC 而非组合标记 | 组合标记在微博显示为 □，ZWC 经测试可存活 |
| 干扰项包裹原文 | 删除破坏原文，比插入假字符更难绕过 |
| Canvas 图片渲染 | 纯前端，无需服务器，跨平台兼容 |
| localStorage | 简单持久化，无需账号或后端 |

---

## 已知局限

- 技术用户可用正则批量删除括号，但干扰项设计使原文出现缺字
- 短文位置有限，工具尽量多插入但无法保证密度
- 零宽字符在部分平台（非微博）可能被过滤
- 验证功能展示证据，无法自动向平台举报

---

*最后更新：2026年4月*
